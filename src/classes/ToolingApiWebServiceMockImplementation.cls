/*
 *Web service mock implementation for the tooling api
 *@author Ximena Lasserre
 *@since October 2015
 */
 @isTest
global class ToolingApiWebServiceMockImplementation implements WebServiceMock {
   global void doInvoke(
           Object stub,
           Object request,
           Map<String, Object> response,
           String endpoint,
           String soapAction,
           String requestName,
           String responseNS,
           String responseName,
           String responseType) {

        // Create response element from the autogenerated class.
        // Populate response element.
        // Add response element to the response parameter, as follows:
        String fieldId = '003G000002ML2QK';
        ToolingAPIWSDL.queryResponse_element response_x = new ToolingAPIWSDL.queryResponse_element();
        ToolingAPIWSDL.QueryResult queryResult = new ToolingAPIWSDL.QueryResult();
        ToolingAPIWSDL.sObject_x querysObject = new ToolingAPIWSDL.sObject_x();
        querysObject.Id = fieldId;
        List<ToolingAPIWSDL.sObject_x> sObjects = new List<ToolingAPIWSDL.sObject_x>();
        sObjects.add(querysObject);
        queryResult.records = sObjects;
        response_x.result = queryResult;
        response.put('response_x', response_x); 
   }
}